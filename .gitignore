# Examples 1:
# Project with only source/config files - no build artifacts, dependencies, or temp files
# Expected: Empty output (nothing added to .gitignore)

# Example 2:
# Project containing build artifacts and temp files
# Expected: Appropriate ignore patterns added

# Current scenario:
# Only HTML files modified - these are source files
# No build artifacts, dependencies, or temp files detected
# No need to add any ignore patterns

# Final decision: Output nothing since only source files are present